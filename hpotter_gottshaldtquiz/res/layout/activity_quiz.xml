<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/RelativeLayout1"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_alignParentLeft="true"
    android:layout_alignParentTop="true"
    android:background="@color/white"
    android:fillViewport="true"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".QuizActivity" >

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:gravity="top|center_horizontal"
        android:orientation="horizontal"
        android:weightSum="5" >

        <!-- TODO: fix android:contentDescription to satisfy accessibility requirements  -->
        <ImageButton
            android:id="@+id/figuresImage1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:contentDescription="@null" 
            android:onClick="buttonClickHandler"
            android:scaleType="fitXY"
            android:src="@drawable/figure1" />

        <!-- TODO: fix android:contentDescription to satisfy accessibility requirements  -->
        <ImageButton
            android:id="@+id/figuresImage2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:contentDescription="@null"
            android:onClick="buttonClickHandler"
            android:scaleType="fitXY"
            android:src="@drawable/figure2" />

        <!-- TODO: fix android:contentDescription to satisfy accessibility requirements  -->
        <ImageButton
            android:id="@+id/figuresImage3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:contentDescription="@null"
            android:onClick="buttonClickHandler"
            android:scaleType="fitXY"
            android:src="@drawable/figure3" />

        <!-- TODO: fix android:contentDescription to satisfy accessibility requirements  -->
        <ImageButton
            android:id="@+id/figuresImage4"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:contentDescription="@null"
            android:onClick="buttonClickHandler"
            android:scaleType="fitXY"
            android:src="@drawable/figure4" />

        <!-- TODO: fix android:contentDescription to satisfy accessibility requirements  -->
        <ImageButton
            android:id="@+id/figuresImage5"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:contentDescription="@null"
            android:onClick="buttonClickHandler"
            android:scaleType="fitXY"
            android:src="@drawable/figure5" />
    </LinearLayout>

    <!-- TODO: fix android:contentDescription to satisfy accessibility requirements  -->
    <ImageView
        android:id="@+id/quizImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/linearLayout1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:adjustViewBounds="true"
        android:contentDescription="@null" />

</RelativeLayout>